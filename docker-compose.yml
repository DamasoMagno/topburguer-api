version: '3.8'

services:
  db:
    image: postgres:15
    ports:
      - 5433:5432
    environment:
      POSTGRES_USER: adonai
      POSTGRES_PASSWORD: adonai
      POSTGRES_DB: adonai
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data: